YUI.add('moodle-availability_grouping-form', function (Y, NAME) {

M.availability_grouping = M.availability_grouping || {};
M.availability_grouping.form = {
};


}, '@VERSION@', {"requires": ["moodle-core_availability-form"]});
